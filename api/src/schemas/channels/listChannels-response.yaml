title: ListChannelsResponse
description:
  The response for the list channels endpoint.
type: object
properties:
  channels:
    description:
      A list of all the channels.
    type: array
    items:
      $ref: ./channel.yaml
required:
  - channels
