title: Pull Channel Operation
allOf:
  - $ref: ./PullOperation.yaml
  - type: object
    required:
      - channel_handle
      - status
      - id
    properties:
      channel_handle:
        type: string
