title: Pull Replies Operation
type: object
required:
  - pull
  - status
  - id
properties:
  pull:
    type: string
    const: replies
  video_id:
    type: string
  comment_id:
    type: string
  estimated_cost:
    type: integer
    readOnly: true
    format: int32
  status:
    type: string
    readOnly: true
    enum:
      - ready
      - fetching
      - successful
      - warning
      - failed
  id:
    type: string
    readOnly: true
